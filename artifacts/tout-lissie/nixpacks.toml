[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install -g pnpm",
  "cd ../.. && pnpm install --frozen-lockfile"
]

[phases.build]
cmds = ["cd ../.. && BASE_PATH=/ pnpm --filter @workspace/tout-lissie run build"]

[start]
cmd = "pnpm --filter @workspace/tout-lissie run start"
